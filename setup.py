from setuptools import setup, find_packages

setup(
    name='ids706_mini_project',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'flake8',
        'pytest',
    ],
)
